#Parser Test program

var z : int = 5 + 5 + 5 + 5 + 5 + 5 * 5;

var x : int = 0;
var y : float = 3;
let HELLO : char = 'a';

def main(a : int, b : float) -> int
{
	alloc(alloc(x));
	x = a + b;
	a + b;
	10 + 10;
	y = b - a;
	return a;
	a << b;
	b >> a;
	
	if(x == 0)
	{
		x = x + 1;
	}
}

def alloc(a : float) -> char
{
	return a;
}